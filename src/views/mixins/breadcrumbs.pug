mixin breadcrumbs(...items)
  nav.breadcrumbs
    .wrapper
      each item, index in items
        if (index === items.length - 1)
          span.breadcrumbs__item= item
        else
          a(href="#").breadcrumbs__item= item
